import styled from 'styled-components'

export const ContainerContent = styled.div`
  height: 100%;

  h3 {
    font-size: 1.4rem;
    padding: 0 !important;
    margin: 0 !important;
    color: var(--text-gray);
    font-weight: 500;
  }
`;