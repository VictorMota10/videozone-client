import styled from "styled-components";

export const ContainerContent = styled.div`
  height: 100%;
  padding-right: 1rem;

  h3 {
    font-size: 1.4rem;
    padding: 0 !important;
    margin: 0 !important;
    color: var(--text-gray);
    font-weight: 500;
  }

  .channel-card {
    width: 16dvw;
    height: 40dvh;

    .cover-card {
      height: 24dvh;
      width: 100%;
    }
  }
`;
